{
  "name": "leads-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["packages/*", "sites/*"],
  "scripts": {
    "dev": "npm run dev -w @leads/tree-service",
    "build": "npm run build --workspaces --if-present",
    "test": "node --test packages/theme/test/*.test.mjs"
  }
}
