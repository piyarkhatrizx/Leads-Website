---
interface Props { title: string; text: string; index: number }
const { title, text, index } = Astro.props
---
<article class="card">
  <span class="num">{String(index + 1).padStart(2, '0')}</span>
  <h3>{title}</h3>
  <p>{text}</p>
</article>

<style>
  .card { background: var(--paper); border: 1px solid var(--line); padding: 26px 24px 24px; }
  .num { color: var(--accent); display: block; font: 11px var(--mono); letter-spacing: 0.1em; margin-bottom: 18px; }
  h3 { font-family: var(--serif); font-size: 25px; }
  p { color: var(--muted); font-size: 14px; margin: 0; }
</style>
